machine:
  php:
    version: 7.0.7
dependencies:
  pre:
    - "bower install"
  cache_directories:
    - "node_modules"
    - "bower_components"
test:
  post:
   - curl 'https://api.rollbar.com/api/1/deploy/' -F access_token=db50fd44ca59427eaf882606637a1085 -F environment=production -F revision="$CIRCLE_BUILD_NUM" -F rollbar_username=ewapettke -F local_username=$CIRCLE_USERNAME -F comment="just a test"
